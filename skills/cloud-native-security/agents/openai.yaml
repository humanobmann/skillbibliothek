interface:
  display_name: Cloud-Native Security
  short_description: Harden Kubernetes and cloud-native delivery end to end
  default_prompt: Use $cloud-native-security to review this workload from source and build provenance through admission and runtime controls.
policy:
  allow_implicit_invocation: true
  products: [CHAT, CODEX]
